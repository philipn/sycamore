= dev to-do =
 * Need to move map references to work locally (based off of config)
 * RSS needs tag for "<whatever> Wiki Robot" instead of Davis Wiki Robot
 * userstats.xml error on comment
 * kill search bug:  make deleted entries deleted from index (write app)
 * new cookie system
 * finalize thumbnails
 * test resulting css on different browsers
 * check out new linking code

= server to-do =
 * so much to do..

dsfjsdkljfhsdlkshf

[[Stats(PhilipNeustrom)]]

["a redirecting page"]

["philip's test page"]
